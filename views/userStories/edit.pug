extends ../navbar

block navbar
  h1 Editar card
  form(name='add-userStory', id='form-update-userStory', method='POST', action=`/projects/${userStory.project}/userStories/${userStory._id}?_method=PUT`)
    include form.pug
